# My App (frontend)

Quick start for the frontend (React + Vite)

Prereqs
- Node.js 16+ and npm or yarn

Install and run locally
```bash
cd my-app
npm install
npm run dev
```

Build for production
```bash
cd my-app
npm run build
```

Notes
- Do not store API secrets in the frontend repository. Use the backend or deployment platform secrets.
